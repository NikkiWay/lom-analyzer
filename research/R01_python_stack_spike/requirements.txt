# Python 3.12 required (fasttext native module does not compile on 3.13+).
# Install fasttext-wheel BEFORE dostoevsky (provides the fasttext module
# from a clean build system; dostoevsky's own fasttext==0.9.2 dep has a
# broken setup.py on Windows).
fasttext-wheel>=0.9.2
dostoevsky>=0.6
natasha>=1.6
pymorphy3>=2.0
langdetect>=1.0.9
sentence-transformers>=2.7
fastapi>=0.110
uvicorn>=0.29
